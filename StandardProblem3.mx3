Aex = 1
Msat = sqrt(2/mu0)
Ku1 = 0.1
anisU = vector(1, 0, 0)

N:= 64
setGridsize(N, N, N)
L:= Lmin
setcellsize(L/N, L/N, L/N)

Lmin := 8.0
Lmax := 9.0
Lstep := 0.05

m = vortex(1, -1)

for L=Lmin; Lmin <= Lmax; Lmin += Lstep{
	setcellsize(L/N, L/N, L/N)
	minimize()
	tablesave()
}
