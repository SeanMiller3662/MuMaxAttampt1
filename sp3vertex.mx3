Aex=1
Msat=sqrt(2/mu0)
anisU = vector(1,0,0)
Ku1 = 0.1
N:=64.0
Lmin := 8
Lmax := 9
Lstep := 0.05
tableadd(E_total)
L := Lmin
tableaddvar(L, "L", "")
for L = Lmin; L <= Lmax; L= L + Lstep{
    
    SetGridSize(N,N,N)

    SetCellSize(L/N,L/N,L/N)
    m = vortex(1, -1)
    minimize()
    tablesave()
}
